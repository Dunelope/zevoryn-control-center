import { BrowserRouter, Route, Routes } from 'react-router-dom'; import { Layout } from './components/Layout'; import { ComingSoon } from './components/ComingSoon'; import { Dashboard } from './pages/Dashboard'; import { Products } from './pages/Products'; import { ProductDetail } from './pages/ProductDetail'; import { Events } from './pages/Events'; import './styles.css';
export function App() { return <BrowserRouter><Routes><Route element={<Layout />}><Route path="/" element={<Dashboard/>}/><Route path="/products" element={<Products/>}/><Route path="/products/:id" element={<ProductDetail/>}/><Route path="/events" element={<Events/>}/>{[['/monitoring','Monitoring'],['/beta','Beta'],['/customers','Customers'],['/billing','Billing'],['/ai-lab','AI Lab']].map(([path,title]) => <Route key={path} path={path} element={<ComingSoon title={title}/>}/>)}</Route></Routes></BrowserRouter>; }
