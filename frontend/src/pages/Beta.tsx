import { FormEvent, useEffect, useState } from 'react'; import { Link } from 'react-router-dom'; import { api } from '../lib/api'; import type { BetaCampaign, Product } from '../lib/types';
export function Beta() { const [campaigns, setCampaigns] = useState<BetaCampaign[]>([]); const [products, setProducts] = useState<Product[]>([]); const [productId, setProductId] = useState(''); const [name, setName] = useState(''); const [max, setMax] = useState('5'); const [error, setError] = useState(''); const load = async () => { try { setCampaigns(await api.betaCampaigns()); setProducts(await api.products()); } catch (e) { setError((e as Error).message); } }; useEffect(() => { void load(); }, []); async function create(e: FormEvent) { e.preventDefault(); try { await api.createBetaCampaign({ productId, name, description: null, maxInvitations: Number(max), startsAtUtc: null, endsAtUtc: null }); setName(''); await load(); } catch (e) { setError((e as Error).message); } } const productName = (id: string) => products.find(p => p.id === id)?.name ?? id; return <section><div className="page-heading"><div><p className="eyebrow">Orchestration</p><h2>Beta Management</h2></div></div>{error && <p className="error">{error}</p>}<div className="grid"><div className="card"><h3>Campaigns</h3>{campaigns.length === 0 ? <p className="muted">No beta campaigns yet.</p> : <ul className="rows">{campaigns.map(c => <li key={c.id}><div><Link to={`/beta/campaigns/${c.id}`}>{c.name}</Link><span>{productName(c.productId)} · {c.totalInvitations}/{c.maxInvitations} used · {c.remainingCapacity} remaining</span></div><span className={`status ${c.status.toLowerCase()}`}>{c.status}</span></li>)}</ul>}</div><form className="card form" onSubmit={create}><h3>Create campaign</h3><select required value={productId} onChange={e => setProductId(e.target.value)}><option value="">Select product</option>{products.map(p => <option key={p.id} value={p.id}>{p.name}</option>)}</select><input required placeholder="Campaign name" value={name} onChange={e => setName(e.target.value)}/><input required min="1" type="number" placeholder="Maximum invitations" value={max} onChange={e => setMax(e.target.value)}/><button type="submit">Create draft</button></form></div></section>; }
